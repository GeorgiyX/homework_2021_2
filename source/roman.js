'use strict';

function roman(inputNum) {
    let inputType = typeof(inputType);
    if (inputType != "string" && inputType != "number") {
        throw TypeError("Неверный тип входных данных!")
    }

}